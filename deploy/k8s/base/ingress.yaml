apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: idrm-demo-claudecode-ingress
  annotations:
    # generic ingress annotation, can be overridden in overlays
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: api.idrm-demo-claudecode.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: idrm-demo-claudecode-api
                port:
                  number: 8888
